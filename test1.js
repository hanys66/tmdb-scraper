/*
const puppeteer = require("puppeteer");

(async () => {
    console.log("Installed Chromium Path:", puppeteer.executablePath());
})();
*/
const puppeteer = require("puppeteer");

(async () => {
    console.log("Installed Chromium Path:", puppeteer.executablePath());
})();